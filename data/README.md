This folder contains datasets used as an input to be able to calculate fairness metrics over values predicted by ML models, RAG systems, helpfulness answers, LLMS as judge scores, et.c
Note for a more production ready the files stored here may be store in AWS  S3 or equivalent for GoogleCloud storage.
